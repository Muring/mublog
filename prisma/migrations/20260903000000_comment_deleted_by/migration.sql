-- 댓글을 누가 지웠는지 남긴다.
--
-- 작성자 본인이 지운 것과 관리자가 지운 것은 읽는 사람에게 뜻이 다르다.
-- 지금까지는 deleted_at 만 있어서 둘을 구분할 방법이 없었다.
--
-- 관계(FK)를 걸지 않는다. 지운 관리자가 나중에 탈퇴하더라도
-- "관리자가 지웠다" 는 사실 자체는 남아야 한다.
ALTER TABLE "comments" ADD COLUMN "deleted_by" UUID;
