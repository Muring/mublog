/**
 * 미디어 쿼리 기준선.
 *
 * `640px` 이 네 파일에 각각 리터럴로 박혀 있었다. 사실상 이 저장소의 모바일
 * 기준선인데, 한 곳을 고쳐도 나머지가 따라오지 않는 상태였다.
 *
 * 여기 없는 값은 그 화면 하나에만 해당하는 것이라 파일에 그대로 둔다.
 * 목록이 1열이 되는 627px 처럼 계산으로 정해진 값은 globals.css 가 갖고 있고,
 * 서로를 가리키는 주석이 붙어 있다.
 */

/** 좁은 화면. 헤더·댓글·사이드 메뉴·토스트가 이 지점에서 함께 접힌다. */
export const mobile = "@media (max-width: 640px)";
