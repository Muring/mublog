import styled from "@emotion/styled";

export const PostGridWrapper = styled.div`
    width: 100vw;
`;

export const GirdList = styled.div`
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
`;
